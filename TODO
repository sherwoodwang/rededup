add logs to track progress
handle missing files in the archive
graceful interrupting
handle errors from another process
handle trailing slashes in symbolic links
add commands to change settings
add forced refresh, ignoring mtime
recover atime if necessary
handle BrokenPipeError with print()
